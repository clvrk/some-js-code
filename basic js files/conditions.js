var myAge = 13;

if(myAge < 18) {
    console.log('You are over 18. ===> ' + `${myAge > 18}`)
    console.log('Then stop playing hookie!')
} // users are given these messages depending on whether they are over 18 or not.
else if(myAge > 18) {
    console.log('You are over 18. ===> ' + `${myAge > 18}`)
    console.log('Then be responsible and have a nice day!')
}